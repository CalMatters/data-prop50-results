[build]
  command = "npm run build"
  publish = "build"

[functions]
  node_bundler = "esbuild"
  directory    = "functions" 

[[headers]]
  for = "/*"
    [headers.values]
    Access-Control-Allow-Origin = "*"